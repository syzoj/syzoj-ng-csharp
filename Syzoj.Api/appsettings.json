{
  "ConnectionStrings": {
    "Database": "User Id=postgres;Host=localhost;Database=syzoj;",
    "Redis": "localhost",
    "RedisCache": "localhost",
    "RabbitMQ": "amqp://guest:guest@localhost:5672/"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "FileSystem": {
    "Type": "Local",
    "Path": "/var/syzoj/uploads",
    "Secret": "secret"
  }
}
